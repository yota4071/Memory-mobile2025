{
  "name": "memory-mobile",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "expo": "^53.0.9",
    "turbo": "^2.5.3"
  },
  "dependencies": {
    "expo-location": "~18.1.5"
  }
}
